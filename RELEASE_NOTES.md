# ChatFLOW 2.1.5

基于 2.1.3 的六项问题修复（用户实测反馈）：

- 修复后台「加载示例模板 / 一键清空示例内容」按钮点击无反应的问题（按钮处理函数原先被包在嵌套 IIFE 内，内联 onclick 无法调用，已提到全局作用域）。
- 修复左上角 Logo 未生效的问题：内置示例模板此前未写入 `site_logo` 配置，导致头部一直显示占位符号；现已在模板配置中写入 logo，加载示例后头部显示真实 Logo。
- 导航栏「Frequently Asked Questions」现统一显示为「FAQ」（modern 模板本就如此，重载示例后即生效）。
- 推广工具箱（灰帽）数据此前未被界面读取展示，现已确认隐藏关键词等数据随示例写入并在生成站点中实际输出；modern 模板自带的 4 列页脚重载后恢复。
- 横幅轮播：轮播脚本与样式本就存在，本次补充了淡入过渡，5 张示例横幅自动切换。
- 版本号统一升至 2.1.5（程序 / 打包配置 / 后台页脚 / 自测断言 / 说明文档）。

以下为沿用的 2.1.3 修复：

修复每次关闭主程序后都要重新输入 GitHub 用户名和 Token 的问题。

- 登录默认勾选“记住账号和 Token”。软件把账号加密保存在本机数据目录，重开主程序时通过短时、一次性启动凭证恢复登录，不依赖窗口缓存。
- 登录 Cookie 同样加密，不在网页中回填或显示 Token。登录资料不随网站生成和产品备份导出；独立窗口沿用原有的隔离缓存方式。
- 可取消记住登录，也可在后台“退出并清除登录”。正常关闭窗口与主动退出账号区分处理。
- 新增连续六次真实窗口启动验收，覆盖记住、关闭重开、退出、退出后重开、不记住及再次重开。电脑整机重启不在自动化实测范围内，不冒充已经实测。
- 日常直接打开主程序，不需要 command/bat 前置步骤；Intel 旧强制浏览器设置可通过配套恢复脚本备份并解除。已验证正常的采集、AI 和窗口功能继续保留。

以下为沿用的 2.1.1 修复：

修复原生窗口没有启动事件循环导致不显示、后端退出的问题。新增实际窗口显示与页面加载验收。

- 采集导入时将图片下载到本机上传目录，描述一并保存；下载失败明确显示数量并保留原地址。生成网站时包含本机图片。旧版已导入的产品不会自动改写。
- SEO 和推广工具增加大白话用途、填写例子和操作步骤。
- 语言保存后同步当前语言；连接断开时提示重新进入软件和确认保存结果。Mac 浏览器启动器使用已安装的程序，并在终端关闭后保持服务。
- Windows 安装包携带 AI 所需的微软运行组件；启动外部引擎时恢复正常 DLL 查找环境，缺失的引擎文件从已下载的压缩包修复。保留实际启动错误码与记录，不再一律提示内存不足或重装语言包。四代 i5 用户机仍需安装后复测。

保留 2.1 系列功能：

- 商品链接批量采集；关键词、店铺公开页面发现链接；保存的 HTML 文件导入。
- 多 SKU、原编码、真实价格、未知库存、来源与采集提示保留到草稿和产品库；同源去重；网站详情页显示规格组合。
- 本地 Qwen3-4B-Instruct-2507 生成系统 11 种语言的待校对译稿，无需翻译 API 账号；可开启采集后自动翻译。首次下载约 2.5GB 模型及运行引擎，建议 8GB 以上内存并预留 6GB 磁盘。原文语言支持自动识别或手动指定；保留数字和型号，原文、译文同时保存。简繁字形通过 OpenCC 转换。
- 隐藏关键词、隐藏链接、正文关键词内链和批量主题页草稿，保存设置后实际参与预览和站点生成。
- 版本恢复保留采集资料与译文。延续 2.0.1 授权 HTTPS 证书修复。

已实测 Shopify 官方演示店公开商品的描述、25 张图片下载及网站生成。其他平台依赖公开页面资料，可能受登录、验证码或动态页面限制；不是全平台完整采集保证。店铺发现上限 3 页 / 30 个链接；单张图片最多 5 MB，支持 JPEG、PNG、GIF、WebP，无法下载的图片需要补传。本地 AI 生成的是待校对译稿，实测部分语言可能译错材质、颜色，导入和发布前请核对；不保证达到人工翻译标准，不自动换汇，不自动上架。推广工具不承诺排名或订单。

模型来源为 [Qwen3-4B-Instruct-2507 原版](https://huggingface.co/Qwen/Qwen3-4B-Instruct-2507)（Apache 2.0），使用社区 [Unsloth 固定版本的 Q4_K_M 转换](https://huggingface.co/unsloth/Qwen3-4B-Instruct-2507-GGUF/tree/a06e946bb6b655725eafa393f4a9745d460374c9)，并非 Qwen 官方 GGUF，下载后核对 SHA-256。运行引擎为官方 [llama.cpp b10894](https://github.com/ggml-org/llama.cpp/tree/b10894)（MIT），[OpenCC](https://github.com/BYVoid/OpenCC) 使用 Apache 2.0 许可。
